{
  "name": "cookmybots-x-bot",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "install:root": "bash -lc \"set -e; if [ -f package-lock.json ]; then npm ci; else npm install; fi\"",
    "dev": "node --watch src/index.js",
    "start": "node src/index.js",
    "build": "npm run install:root && echo build"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "mongodb": "^6.10.0",
    "grammy": "^1.20.0"
  },
  "devDependencies": {}
}
